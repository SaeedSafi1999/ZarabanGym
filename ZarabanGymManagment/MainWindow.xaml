<Window x:Class="ZarabanGym.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Zaraban Gym"
        Height="700"
        Width="1200"
        WindowStartupLocation="CenterScreen"
        Background="#0F172A">

    <Window.Resources>

        <!-- TextBox Style -->
        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Height" Value="50"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="#1E293B"/>
            <Setter Property="BorderBrush" Value="#334155"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Padding" Value="15,0"/>
            <Setter Property="Margin" Value="0,10"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border"
                                CornerRadius="12"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ScrollViewer Margin="5"
                                          x:Name="PART_ContentHost"/>
                        </Border>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="border"
                                        Property="BorderBrush"
                                        Value="#38BDF8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- PasswordBox Style -->
        <Style x:Key="ModernPasswordBox" TargetType="PasswordBox">
            <Setter Property="Height" Value="50"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="#1E293B"/>
            <Setter Property="BorderBrush" Value="#334155"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Padding" Value="15,0"/>
            <Setter Property="Margin" Value="0,10"/>
        </Style>

        <!-- Button Style -->
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Height" Value="55"/>
            <Setter Property="FontSize" Value="17"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="#0EA5E9"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>

            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="btnBorder"
                                Background="{TemplateBinding Background}"
                                CornerRadius="14">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>

                        <ControlTemplate.Triggers>

                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="btnBorder"
                                        Property="Background"
                                        Value="#0284C7"/>
                            </Trigger>

                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="btnBorder"
                                        Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="0.97"
                                                        ScaleY="0.97"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>

                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </Window.Resources>

    <Grid>

        <!-- Background Gradient -->
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#020617" Offset="0"/>
                <GradientStop Color="#0F172A" Offset="1"/>
            </LinearGradientBrush>
        </Grid.Background>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="450" />
        </Grid.ColumnDefinitions>

        <!-- Left Side -->
        <Grid>

            <!-- Decorative Circles -->
            <Ellipse Width="500"
                     Height="500"
                     Fill="#0EA5E9"
                     Opacity="0.08"
                     HorizontalAlignment="Left"
                     VerticalAlignment="Top"
                     Margin="-150,-100,0,0"/>

            <Ellipse Width="400"
                     Height="400"
                     Fill="#8B5CF6"
                     Opacity="0.08"
                     HorizontalAlignment="Right"
                     VerticalAlignment="Bottom"
                     Margin="0,0,-100,-100"/>

            <StackPanel VerticalAlignment="Center"
                        HorizontalAlignment="Center">

                <!-- SVG Like Logo -->
                <Viewbox Width="250"
                         Height="250">

                    <Canvas Width="200"
                            Height="200">

                        <Path Fill="#38BDF8"
                              Data="M100,10
                                    A90,90 0 1 1 99,10 Z"/>

                        <Path Fill="#0F172A"
                              Data="M100,40
                                    A60,60 0 1 1 99,40 Z"/>

                        <TextBlock Text="ZG"
                                   Foreground="White"
                                   FontSize="55"
                                   FontWeight="Bold"
                                   Canvas.Left="55"
                                   Canvas.Top="70"/>
                    </Canvas>

                </Viewbox>

                <TextBlock Text="ZARABAN GYM"
                           Foreground="White"
                           FontSize="38"
                           FontWeight="Bold"
                           HorizontalAlignment="Center"/>

                <TextBlock Text="Management System"
                           Foreground="#94A3B8"
                           FontSize="18"
                           HorizontalAlignment="Center"/>
            </StackPanel>

        </Grid>

        <!-- Login Card -->
        <Border Grid.Column="1"
                Width="380"
                Padding="35"
                CornerRadius="25"
                Background="#1AFFFFFF"
                BorderBrush="#20FFFFFF"
                BorderThickness="1"
                HorizontalAlignment="Center"
                VerticalAlignment="Center">

            <StackPanel>

                <TextBlock Text="Welcome Back"
                           FontSize="30"
                           FontWeight="Bold"
                           Foreground="White"
                           Margin="0,0,0,5"/>

                <TextBlock Text="Login to continue"
                           Foreground="#94A3B8"
                           Margin="0,0,0,25"/>

                <TextBlock Text="Username"
                           Foreground="White"/>

                <TextBox Style="{StaticResource ModernTextBox}"/>

                <TextBlock Text="Password"
                           Foreground="White"
                           Margin="0,10,0,0"/>

                <PasswordBox Style="{StaticResource ModernPasswordBox}"/>

                <Button Content="Login"
                        Style="{StaticResource ModernButton}"
                        Margin="0,25,0,0"/>

            </StackPanel>

        </Border>

    </Grid>
</Window>

<!--<Window x:Class="ZarabanGym.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Zaraban Gym"
        Height="700"
        Width="1200"
        WindowStartupLocation="CenterScreen"
        Background="#FFF6FA">

    <Window.Resources>

        -->
<!-- TextBox Style -->
<!--
        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Height" Value="50"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Foreground" Value="#6B4453"/>
            <Setter Property="Background" Value="#FFF9FB"/>
            <Setter Property="BorderBrush" Value="#E7B7C8"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Padding" Value="15,0"/>
            <Setter Property="Margin" Value="0,10"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border"
                                CornerRadius="12"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ScrollViewer Margin="5"
                                          x:Name="PART_ContentHost"/>
                        </Border>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="border"
                                        Property="BorderBrush"
                                        Value="#D89CA8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        -->
<!-- PasswordBox Style -->
<!--
        <Style x:Key="ModernPasswordBox" TargetType="PasswordBox">
            <Setter Property="Height" Value="50"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Foreground" Value="#6B4453"/>
            <Setter Property="Background" Value="#FFF9FB"/>
            <Setter Property="BorderBrush" Value="#E7B7C8"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Padding" Value="15,0"/>
            <Setter Property="Margin" Value="0,10"/>
        </Style>

        -->
<!-- Button Style -->
<!--
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Height" Value="55"/>
            <Setter Property="FontSize" Value="17"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="#D89CA8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>

            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="btnBorder"
                                Background="{TemplateBinding Background}"
                                CornerRadius="14">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>

                        <ControlTemplate.Triggers>

                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="btnBorder"
                                        Property="Background"
                                        Value="#C47C90"/>
                            </Trigger>

                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="btnBorder"
                                        Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="0.97"
                                                        ScaleY="0.97"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>

                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </Window.Resources>

    <Grid>

        -->
<!-- Background Gradient -->
<!--
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#FFF9FC" Offset="0"/>
                <GradientStop Color="#FFEAF3" Offset="1"/>
            </LinearGradientBrush>
        </Grid.Background>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="450" />
        </Grid.ColumnDefinitions>

        -->
<!-- Left Side -->
<!--
        <Grid>

            -->
<!-- Decorative Circles -->
<!--
            <Ellipse Width="500"
                     Height="500"
                     Fill="#E7B7C8"
                     Opacity="0.08"
                     HorizontalAlignment="Left"
                     VerticalAlignment="Top"
                     Margin="-150,-100,0,0"/>

            <Ellipse Width="400"
                     Height="400"
                     Fill="#F3C6D3"
                     Opacity="0.08"
                     HorizontalAlignment="Right"
                     VerticalAlignment="Bottom"
                     Margin="0,0,-100,-100"/>

            <StackPanel VerticalAlignment="Center"
                        HorizontalAlignment="Center">

                -->
<!-- SVG Like Logo -->
<!--
                <Viewbox Width="250"
                         Height="250">

                    <Canvas Width="200"
                            Height="200">

                        <Path Fill="#D89CA8"
                              Data="M100,10
                                    A90,90 0 1 1 99,10 Z"/>

                        <Path Fill="#FFF6FA"
                              Data="M100,40
                                    A60,60 0 1 1 99,40 Z"/>

                        <TextBlock Text="ZG"
                                   Foreground="#8B5E6C"
                                   FontSize="55"
                                   FontWeight="Bold"
                                   Canvas.Left="55"
                                   Canvas.Top="70"/>
                    </Canvas>

                </Viewbox>

                <TextBlock Text="ZARABAN GYM"
                           Foreground="#8B5E6C"
                           FontSize="38"
                           FontWeight="Bold"
                           HorizontalAlignment="Center"/>

                <TextBlock Text="Management System"
                           Foreground="#B98C9B"
                           FontSize="18"
                           HorizontalAlignment="Center"/>
            </StackPanel>

        </Grid>

        -->
<!-- Login Card -->
<!--
        <Border Grid.Column="1"
                Width="380"
                Padding="35"
                CornerRadius="25"
                Background="#CCFFFFFF"
                BorderBrush="#40E7B7C8"
                BorderThickness="1"
                HorizontalAlignment="Center"
                VerticalAlignment="Center">

            <StackPanel>

                <TextBlock Text="Welcome Back"
                           FontSize="30"
                           FontWeight="Bold"
                           Foreground="#8B5E6C"
                           Margin="0,0,0,5"/>

                <TextBlock Text="Login to continue"
                           Foreground="#B98C9B"
                           Margin="0,0,0,25"/>

                <TextBlock Text="Username"
                           Foreground="#8B5E6C"/>

                <TextBox Style="{StaticResource ModernTextBox}"/>

                <TextBlock Text="Password"
                           Foreground="#8B5E6C"
                           Margin="0,10,0,0"/>

                <PasswordBox Style="{StaticResource ModernPasswordBox}"/>

                <Button Content="Login"
                        Style="{StaticResource ModernButton}"
                        Margin="0,25,0,0"/>

            </StackPanel>

        </Border>

    </Grid>
</Window>-->